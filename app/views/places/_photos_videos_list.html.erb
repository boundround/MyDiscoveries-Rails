<% if @videos.present? %>
    <a name="videos"></a>
    <section class="your-stories video-show">
    <div class="text-center">
      <h4>Videos</h4>
    </div>
      <div class="row"  id="videos-list">
        <%= render partial: 'featured_videos_list', collection: @videos, as: :video %>
      </div>
    <% unless last_page? @videos %>
      <div class="text-center clearfix btm20 single-pagination-container" id="videos-single-pagination-container" style="clear:both">
        <div class="col-lg-12 col-md-12 btm15 hidden-lg single-pagination-loader" align="center">
          <img src="<%= asset_path('loading.gif') %>" width="64" height="64">
        </div>
        <%= link_to "Load More", paginate_videos_place_path(@place, active_videos: @videos.next_page, resources: 'videos', pagination_template: 'featured_videos_list'), class: 'btn btn-default single-pagination btn btn-default top5 load-more', remote: true %>
      </div>
    <% end %>
    </section>
  <% end %>


<% unless @photos.blank? %>
  <a name="photos"></a>
  <section class="your-stories video-show photo">
  <h4 class="text-center">
    Photos
  </h4>
    <div class="row" id="photos-list">
      <%= render partial: 'featured_photos_list', collection: @photos, as: :photo %>
    </div> <!-- row -->
    <% unless last_page? @photos %>
      <div class="text-center clearfix btm20 single-pagination-container" id="photos-single-pagination-container" style="clear:both">
        <div class="col-lg-12 col-md-12 btm15 hidden-lg single-pagination-loader" align="center">
          <img src="<%= asset_path('loading.gif') %>" width="64" height="64">
        </div>
        <%= link_to "Load More", paginate_photos_place_path(@place, active_photos: @photos.next_page, resources: 'photos', pagination_template: 'featured_photos_list'), class: 'btn btn-default single-pagination btn btn-default top5 load-more', remote: true %>
      </div>
    <% end %>
</section>
<% end %>