<div class="container">
  <h2>Edit Primary Category</h2>
  <%= simple_form_for @primary_category do |f| %>
    <%= f.input :name, input_html: { class: 'form-control' } %>
    <%= f.input :icon, as: :file %>
    <%= f.submit :submit %>
  <% end %>
</div>
