<p style="padding-top:50px;">
  <a href="<%= story_places_path(@story) %>" class="btn btn-primary" >Associated Places</a>
  <a href="<%= story_countries_path(@story) %>" class="btn btn-primary" >Associated Countries</a>
  <a href="<%= story_subcategories_path(@story) %>" class="btn btn-primary" target="blank">Choose Categories</a>
  <a href="<%= edit_story_path(@story) %>" class="btn btn-primary" >Edit Post</a>
  <a href="<%= choose_hero_story_path(@story) %>" class="btn btn-primary" >Add/Edit Hero Image</a>
  <a href="<%= story_path(@story) %>" class="btn btn-primary" target="_blank">View Story Page</a>
</p>
