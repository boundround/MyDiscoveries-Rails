<% if @user == current_user || current_user.admin %>
<%= render 'user_nav' %>
         <div class="container-fluid br_container_tab">
             <div class="row br_profile_header hidden-xs">
                 <div class="col-sm-8 col-sm-offset-2">
                     <div class="row">
                         <div class="col-sm-3 col-sm-offset-1 large-avatar-container">
                             <div class="br_shell col-sm-offset-3">
                              <a href="#" data-toggle="modal" data-target="#br_modal">
                                  <% if @user.avatar_url %>
                                    <img src="<%= current_user.avatar_url %>" class="img-circle img-responsive large-avatar" alt="">
                                  <% else %>
                                    <i class="fa fa-user fa-5x"></i>
                                  <% end %>
                              </a>
                            </div>
                         </div>
                         <div class="col-sm-8 br_profile_detail">
                             <div class="row">
                                 <div class="col-md-12">
                                    <div class="row">
                                        <div class="col-sm-11 col-sm-offset-1">
                                            <h2><%= @user.name %><br>
                                                <span>Member Since <%= @user.created_at.strftime("%Y") %></span>
                                            </h2>
                                        </div>
                                    </div>
                                 </div>
                             </div>
                             <div class="row">
                             </div>
                         </div>
                     </div>
                 </div>
             </div>
             <div class="row br_tabs">
                     <!-- Nav tabs -->
                     <div class="col-sm-4">
                         <ul class="nav row br_no_row nav-tabs" role="tablist">
                             <li role="presentation" class="active"><a href="#home" aria-controls="home" role="tab" data-toggle="tab">Summary</a></li>
                             <!-- <li role="presentation"><a href="#profile" aria-controls="profile" role="tab" data-toggle="tab">Transactions</a></li> -->
                         </ul>
                     </div>
                 </div>
             <div class="row">
                     <!-- Tab panes -->
                    <div class="col-sm-12">
                        <div class="tab-content">
                            <div role="tabpanel" class="tab-pane active" id="home">
                                <div class="row">
                                    <div class="col-sm-4 ">
                                        <div class="row br_no_row br_sidebar">
                                            <div class="col-sm-12">
                                                <h1>
                                                  <% if @user.points_balance %>
                                                      <%= @user.points_balance.balance.to_s %>
                                                    <% else %>
                                                      0
                                                    <% end %>
                                                    <br><span>Total Points</span>
                                                </h1>
                                                <h3>Leaderboard</h3>
                                                <ul class="list-unstyled br_Leaderboard">
                                                  <% @leaderboard.each_with_index do |leader, index| %>
                                                    <li>
                                                      <a href="">
                                                      <span class="br_avtar">
                                                        <% if leader.user.avatar.blank? %>
                                                          <img src="<%= asset_path('Badge.png') %>" alt="">
                                                        <% else %>
                                                          <img src="<%= leader.user.avatar_url %>" alt="">
                                                        <% end %>
                                                          <span>
                                                              <%= leader.balance %>
                                                          </span>
                                                      </span>
                                                      <span class="br_avtar_no">
                                                          <%= (index + 1).to_s %>
                                                      </span>
                                                      <span class="br_user_name"><%= strip_domain_from_username(leader.user) %></span>
                                                      </a>
                                                    </li>
                                                    <% end %>
                                                </ul>
                                               <!-- <div class="row visible-xs">
                                                   <a href="#" class="btn btn-block btn-default br_load_more_side">Load More</a>
                                               </div> -->
                                            </div>
                                        </div>

                                    </div>
                                    <div class="col-sm-8">
                                        <div class="row hidden-xs">
                                            <div class="col-sm-6">
                                              <style>
                                                #br_user_photos {
                                                  <% unless @user.user_photos.blank? %>
                                                    background: url("<%= @user.user_photos[rand(@user.user_photos.length - 1)].path_url(:small) %>") no-repeat;
                                                  <% end %>
                                                  background-size: cover;
                                                }
                                              </style>
                                                <div class="row br_user_activity br_bor_top_bot" id="br_user_photos">
                                                    <a href="/users/photos" class="br_ellipse"><%= @user.user_photos.active.length %><br/><span>Photos</span></a>
                                                    <a href="#" data-toggle="modal" data-target="#myModal" class="br_point_earn">Add New<br/><span><%= calculate_user_points_for_asset(@user.user_photos.active) %>PTS</span></a>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                              <style>
                                                #br_user_stories {
                                                  <% unless @user.user_photos.blank? %>
                                                    background: url("<%= @user.user_photos[rand(@user.user_photos.length - 1)].path_url(:small) %>") no-repeat;
                                                  <% end %>
                                                  background-size: cover;
                                                }
                                              </style>
                                               <div class="row br_user_activity br_user_upload br_bor_all" id="br_user_stories">
                                                   <a href="/users/stories" class="br_ellipse"><%= @user.stories.active.length %><br/><span>Stories</span></a>
                                                   <a href="#" data-toggle="modal" data-target="#storyModal" class="br_point_earn">Add New<br/><span><%= calculate_user_points_for_asset(@user.stories.active) %>PTS</span></a>
                                               </div>
                                            </div>
                                        </div>
                                        <div class="row hidden-xs">
                                            <div class="col-sm-6">
                                              <style>
                                                #br_user_reviews {
                                                  <% unless @user.user_photos.blank? %>
                                                    background: url("<%= @user.user_photos[rand(@user.user_photos.length - 1)].path_url(:small) %>") no-repeat;
                                                  <% end %>
                                                  background-size: cover;
                                                }
                                              </style>
                                                <div class="row br_user_activity" id="br_user_reviews">
                                                    <a href="/users/reviews" class="br_ellipse"><%= @user.reviews.active.length %><br/><span>Reviews</span></a>
                                                    <a href="/users/reviews" class="br_point_earn">My Reviews<br/><span><%= calculate_user_points_for_asset(@user.reviews.active) %>PTS</span></a>
                                                </div>
                                            </div>
                                            <div class="col-sm-6">
                                                <div class="row br_user_activity br_bg_purple br_bor_all">
                                                    <h1><a href="#">Share reviews &amp; stories to earn points</a></h1>
                                                </div>
                                            </div>
                                        </div
                                        <div class="row visible-xs">
                                            <div class="col-xs-12">
                                                <div id="owl-example" class="owl-carousel">
                                                    <div>
                                                        <div class="col-sm-12">
                                                          <style>
                                                            #br_user_mobile_photos {
                                                              <% unless @user.user_photos.blank? %>
                                                                background: url("<%= @user.user_photos[rand(@user.user_photos.length - 1)].path_url(:small) %>") no-repeat;
                                                              <% end %>
                                                              background-size: cover;
                                                            }
                                                          </style>
                                                          <div class="row br_user_activity" id="br_user_mobile_photos">
                                                              <a href="/users/photos" class="br_ellipse"><%= @user.user_photos.active.length %><br/><span>Photos</span></a>
                                                              <a href="#"  data-toggle="modal" data-target="#myModal" class="br_point_earn">Add New<br/><span><%= calculate_user_points_for_asset(@user.user_photos.active) %>PTS</span></a>
                                                          </div>
                                                        </div>
                                                    </div>
                                                    <div>
                                                      <div class="col-sm-12">
                                                        <style>
                                                          #br_user_mobile_stories {
                                                            <% unless @user.user_photos.blank? %>
                                                              background: url("<%= @user.user_photos[rand(@user.user_photos.length - 1)].path_url(:small) %>") no-repeat;
                                                            <% end %>
                                                            background-size: cover;
                                                          }
                                                        </style>
                                                      <div class="row br_user_activity br_user_upload" id="br_user_mobile_stories">
                                                          <a href="/users/stories" class="br_ellipse"><%= @user.stories.active.length %><br/><span>Stories</span></a>
                                                           <a href="#"  data-toggle="modal" data-target="#storyModal" class="br_point_earn">Add New<br/><span><%= calculate_user_points_for_asset(@user.stories.active) %>PTS</span></a>
                                                      </div>
                                                    </div>
                                                    </div>
                                                    <div>
                                                        <div class="col-sm-12">
                                                          <style>
                                                            #br_user_mobile_reviews {
                                                              <% unless @user.user_photos.blank? %>
                                                                background: url("<%= @user.user_photos[rand(@user.user_photos.length - 1)].path_url(:small) %>") no-repeat;
                                                              <% end %>
                                                              background-size: cover;
                                                            }
                                                          </style>
                                                          <div class="row br_user_activity" id="br_user_mobile_review">
                                                              <a href="/users/reviews" class="br_ellipse"><%= @user.reviews.active.length %><br/><span>Reviews</span></a>
                                                              <a href="#" class="br_point_earn">Add New<br/><span><%= calculate_user_points_for_asset(@user.reviews.active) %>PTS</span></a>
                                                          </div>
                                                        </div>
                                                    </div>
                                                    <div class="col-sm-12">
                                                        <div class="row br_user_activity br_bg_purple ">
                                                            <h1><a href="#">Share reviews &amp; stories to earn points</a></h1>
                                                        </div>
                                                    </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <!-- <div role="tabpanel" class="tab-pane" id="profile"><h1>Pending</h1></div> -->
                        </div>
                    </div>
             </div>
         </div>

 <div class="modal fade" id="storyModal">
  <div class="modal-dialog">
    <div class="modal-content">
      <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
      <%= form_for @story, url: stories_profile_create_path, :html => { :id => "story-form" } do |f| %>
        <%= f.fields_for :user_photos do |ff| %>
          <div class="img-cont share">
            <div class="story-image-container" id="story-image-1-container">
              <img id="story-image-1" class="story-image-preview" src="#" alt="Your Image" />
            </div>
            <div class="share-btn">
              <a class="btn btn-default"><span class="fa fa-picture-o"></span>
                <br>Click To Select Image
                <%= ff.file_field :path, label: false %>
              </a>
              <%= ff.hidden_field :user_id, :value => (current_user.id if user_signed_in?) %>
              <%= ff.hidden_field :status, :value => "draft" %>
              <%= ff.hidden_field :story_priority, :value => 1 %>
            </div>
          </div>
        <% end %>
        <div class="mid-cont">
          <h1 class="text-center">
            <%= f.text_area :title, id: "story_headline", placeholder: "Story Title Goes Here", maxlength: 40 %>
          </h1>
          <h3 class="text-center">by <%= strip_domain_from_username(current_user) if user_signed_in? %></h3>
          <div class="mid-cont">
            <div class="pull-left map-container side-img-cont share">
              <input id="pac-input" class="controls" type="text"
              placeholder="Enter a location">
              <%= f.hidden_field :google_place_id, data: {"geo" => "place_id"}, class: 'form-control', label: "Google Place ID" %>
              <div class="user-gen-map"></div>
            </div>
            <div class="pull-right side-img-cont share">
              <div class="story-image-container" id="story-image-2-container">
                <img id="story-image-2" class="story-image-preview" src="#" alt="Your Image" />
              </div>
              <div class="share-btn">
                <%= f.fields_for :user_photos do |ff| %>
                  <a class="btn btn-default"><span class="fa fa-picture-o"></span>
                    <br>Click To Select Image
                    <%= ff.file_field :path, label: false %>
                  </a>
                  <%= ff.hidden_field :user_id, :value => (current_user.id if user_signed_in?) %>
                  <%= ff.hidden_field :status, :value => "draft" %>
                  <%= ff.hidden_field :story_priority, :value => 2 %>
                <% end %>
                </div>
              </div>
              <!-- <span class="text-cont" id="editor1" contenteditable="true" name="story[content]" role="textbox">
                Replace this text with your story.
              </span> -->
              <%= f.text_area :content, placeholder: "Write your story here.", maxlength: 4000 %>
              <%= f.hidden_field :user_id, :value => (current_user.id if user_signed_in?) %>
              <%= f.hidden_field :status, :value => "draft" %>
              <div class="clearfix"></div>
              <div class="slider-cont">
                <div>
                  <!-- <h4>Heading style looks like this</h4>
                  <p id="myedit" >
                      consectetur adipiscing elit. Vivamus cursus purus quis tellus adipiscing sit amet suscipit justo malesuada. Aenean non tortor id leo consectetur euismod at sed metus. Vivamus sollicitudin ante sit amet arcu faucibus vulputate. Sed non libero odio, non viverra nisl. Vivamus et ligula lorem. Sed pellentesque tincidunt purus in imperdiet. Donec bibendum scelerisque cursus.
                  </p> -->
                  <div class="img-cont2 share z-up">
                    <div class="story-image-container" id="story-image-3-container">
                      <img id="story-image-3" class="story-image-preview" src="#" alt="Your Image" />
                    </div>
                   <div class="share-btn">
                    <%= f.fields_for :user_photos do |ff| %>
                      <a class="btn btn-default"><span class="fa fa-picture-o"></span>
                        <br>Click To Select Image
                        <%= ff.file_field :path, label: false %>
                      </a>
                      <%= ff.hidden_field :user_id, :value => (current_user.id if user_signed_in?) %>
                      <%= ff.hidden_field :status, :value => "draft" %>
                      <%= ff.hidden_field :story_priority, :value => 3 %>
                    <% end %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
            <hr/>
            <div class="br_modal-footer text-center">
              <%= f.submit "Publish story", class: "btn btn-lg", id: "story-publish-button" %>
              <a href="#" data-dismiss="modal" aria-label="Close" >Cancel</a>
            </div>
          </div>
        </div>
      <% end %>
    </div><!-- /.modal-content -->
  </div>
</div>

<div class="modal fade br_add_pic" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
  <div class="modal-dialog text-center" role="document">
   <img src="<%= asset_path('Badge.png') %>"/>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">×</span></button>
   <h1>Add a photo</h1>
   <div class="modal-content">
      <div class="br_upload">
        <%= form_for UserPhoto.new, url: user_photos_profile_create_path, :html => { :id => "user-profile-photo-upload"  } do |f| %>
          <%= f.file_field :path, label: false %>
          <%= f.hidden_field :user_id, :value => (current_user.id if user_signed_in?) %>
          <%= f.hidden_field :status, :value => "draft" %>

        <h4>Click <span class="br_underline">Browse</span> and navigate to the file,<br/> or, just drag the file <strong>here</strong></h4>
        <img id="image-preview" class="user-upload-image-preview" src="#" alt="Your Image" />
      </div>
      <%= f.text_field :caption, label: false, placeholder: "Enter a caption for your photo", class: "form-group user-photo-caption-form" %>
      <div>
        <input id="pac-input-2" class="controls" type="text"
        placeholder="Where was this taken?">
        <%= f.hidden_field :google_place_id, data: {"geo" => "place_id"}, class: 'form-control', label: "Google Place ID" %>
        <div class="user-gen-map"></div>
      </div>
      <div id="user-photo-upload-button">

      </div>
      <div id="progress">
          <div class="bar" style="width: 0%;"></div>
      </div>
      <% end %>
    </div>
  </div>
</div>
<script>
  $(document).ready(function(){
    var carousel = $('#owl-example');
    carousel.wrap("<div class='visible-xs'></div>");
  });
</script>
<style>
.pac-container {
    z-index: 9999 !important;
}
</style>

<%= javascript_include_tag 'user_gen_map.js' %>
<% else %>
  <h1>You must be logged into view this page</h1>
<% end %>

